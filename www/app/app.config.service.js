angular.module('app')
	.service('AppConfig', function($http){
		// TODO: Provide various method to read settings from app.config.json
		
		get_rest_api_url = function(callback){
			// TODO: 1. Read app.config.json. 
			//		 2. Evaluate "use_fake_json" to determine the url
		}
	});